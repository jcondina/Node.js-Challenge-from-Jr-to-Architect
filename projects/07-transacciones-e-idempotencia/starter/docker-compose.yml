services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: project_07
    ports:
      - "5407:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d project_07"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "6307:6379"
